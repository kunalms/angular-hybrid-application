'use strict';

describe('myApp.register module', function() {

  beforeEach(module('myApp.register'));

  describe('register controller', function(){

    it('should ....', inject(function($componentController) {
      //spec body
      var registerCtrl = $componentController('registerComponent');
      expect(registerCtrl).toBeDefined();
    }));

  });
});
